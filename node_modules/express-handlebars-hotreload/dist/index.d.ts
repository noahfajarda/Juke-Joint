import { engine, create } from "./lib";
export { engine, create };
export declare function __hotreload({ port, _glob, }: {
    port?: number;
    _glob?: string;
}): number;
export declare function hotreload(options?: {
    port?: number;
    glob?: string;
}): void;
